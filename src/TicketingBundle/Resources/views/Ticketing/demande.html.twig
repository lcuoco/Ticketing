{% extends "TicketingBundle:Ticketing:layout.html.twig" %}
{% block logout %}<ul class="nav navbar-nav navbar-right"><li><a href="{{ path('ticketing_admin') }}"><span class="glyphicon glyphicon-bullhorn"></span> Interface Administrateur</a></li>
<ul class="nav navbar-nav navbar-right"><li class = "active"><a href="{{ path('demandes_view') }}"><span class="glyphicon glyphicon-menu-hamburger"></span> Toutes les demandes</a></li>
    <ul class="nav navbar-nav navbar-right"><li><a href="{{ path('ticketing_tech') }}"><span class="glyphicon glyphicon-wrench"></span> Interface Technicien</a></li>



        <ul class="nav navbar-nav navbar-right"><li><a href="{{ path('ticketing_inscription_tech') }}"><span class="glyphicon glyphicon-plus"></span> Inscrire un nouveau technicien</a></li>

            <li><a href="{{ path('ticketing_profiles') }}"><span class="glyphicon glyphicon-user"></span> Gérer les Utilisateurs</a></li><ul class="nav navbar-nav navbar-right"><li><a href="{{ path('logout') }}"><span class="glyphicon glyphicon-log-in"></span> Logout</a></li></ul>{% endblock %}


{% block body %}
    <ul>
        <h3>Toutes Les Demandes </h3>

        <div class="container">

            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Objet</th>
                    <th>Description</th>
                    <th>Etat</th>
                    <th> Demandeur</th>
                    <th>Attribué à</th>
                    <th> Date</th>

                </tr>
                {% if listTDemandes %}
                {% for demande in listTDemandes %}
                   <tr>
                <td>{{ demande.objet }}</td>
                    <td>{{ demande.description }}</td>
                    <td>{{ demande.etat }}</td>
                    <td>{{ demande.utilisateur.username }}</td>
                    <td>Pas encore attribué</td>
                    <td>{{  demande.date|date('Y-m-d') }}</td>
                    <td> <a href="{{ path('demande_view', {'page': demande.id}) }}"> Voir la Demande
                        </a></td>
                        </tr>
                        {% endfor %}
                {% endif %}

                {% for ticket in listTickets %}
                </thead>
                <tbody>
                <a>
                    <td>{{ ticket.demande.objet }}</td>
                    <td>{{ ticket.demande.description }}</td>
                    <td>{{ ticket.demande.etat }}</td>
                    <td>{{ ticket.demande.utilisateur.username }}</td>
                    <td>{{ ticket.utilisateur.username }}</td>
                    <td>{{  ticket.demande.date|date('Y-m-d') }}</td>
                    <td> <a href="{{ path('demande_view', {'page': ticket.demande.id}) }}"> Voir la Demande

                        </a></td>

                    {% endfor %}
                </tbody>
            </table>
        </div>



    </ul>
{% endblock %}